---
title: A post still in progress
subtitle: You should only be able to find this one by its direct link
draft: true
---

If you can see this, you followed a direct link to `/blog/draft-test`.

This post is flagged `draft: true` in its front matter, so it's deliberately left out of the `/blog` index, but still renders in full (with the banner above) so you can preview it or send it for proofreading before it's ready. Flip `draft` to `false` (or remove the line) when it is, add a `written` date, and it'll show up on the index like any other post.
