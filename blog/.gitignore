vendor/
composer.phar
cache/*.html

# Real content and site config - gitignored so a fresh clone starts blank.
# See example-posts/ and config.example.php for the committed fallbacks.
posts/
config.php